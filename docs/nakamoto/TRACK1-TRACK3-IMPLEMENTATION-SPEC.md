# Track-1 (re-exec primary) + Track-3 (kill k1-absolute) — Consolidated Implementation Spec

**Status (2026-07-01): DESIGN COMPLETE + adversarially verified. All headline blockers RESOLVED. Next = IMPLEMENT.**
Supersedes the stale bits of `SHARDING-PRODUCTION-READINESS-PLAN.md` (§3 mis-framings corrected here) and
`GENESIS-DENSITY-PHASE2-REORG-AUDIT.md` (materially stale on k2/line-numbers — corrected here).

**Provenance:** produced by 3 adversarial-verify Workflows + 1 gate-builder agent this session. **DO NOT re-run them** — conclusions captured below.
Run IDs (resume only if truly needed): Track-1 spec `wf_9200c8f8-0ee`; Track-3 spec `wf_2a2384ee-785`; 1a proof `wf_f60d4bcf-80b`.

---

## 0. GOAL
Cross-shard interactions between L2 currency-app metagraphs, sequenced by gl0, with the framework token model **RE-EXECUTED + enforced by the hypergraph** (not authoritative-push), at latency not gated by the full k1 challenge window.

## 1. CORRECTED role-split model (I mis-framed this as a binary — it is a ROLE SPLIT, settled for weeks)
- **Metagraph (ml0)** runs `accept()` → its snapshot (the real state transition).
- **Committee (K_S) + watchtowers (VRF-sampled)** RE-RUN the same `accept()` over the pinned base → committee derives+attests, watchtowers verify+fraud-proof. = framework/token-model ENFORCEMENT.
- **gl0 + everyone else** ADOPT the byteDiff (`reconstructInfoFromDiff` over the pinned prior) → reconstruct Info WITHOUT re-executing. `deriveAdoptedCurrencyInfo` (GSCEP:429) dies → byteDiff-adopt.
- Checkpoint carries BOTH (ShardCheckpointProducer:614-682): `includedSnapshots` (= currency incremental snapshots = re-exec INPUT) AND `perMetagraphStateDiff`/`perMetagraphMptRoots` (= `ShardCurrencyStateDiff{upserts,removals}` = the CLAIM). Structure: ml0 binary ∈ `includedSnapshots[mgAddr]` ∈ ShardCheckpoint (per-shard mini-chain, `parentCheckpointHash`+`shardOrdinal`) which rides into gl0 at `gl0AnchorOrdinal` ("this ord or later", loose). Diagrams: `docs/nakamoto/diagrams/{1-shard-to-hypergraph,2-metagraph-to-shard,3-combined-resolution-and-finality}.png`.

## 2. CORRECTED finality model
- Fork choice (`ChainSelection`): shallow (≤kLookback) = **LONGEST chain** (ordinal→slot→VRF, maxvalid-tk, :180) — NOT "densest". Deep (>kLookback) = **DENSITY** (maxvalid-bg, :197-216).
- Triggers (`FinalityTrigger:14-16`): Phase1→2 = max-of{`T_weight`(2/3 stake), `T_count`(2/3 validator count), `T_depth1`(depth-k1)}; Phase2→3 = `T_depth2`(k2). **max-of STAYS**; the problem was never max-of — it was "k1 = absolute floor" (Track-3 fixes that).
- **k1 = operational finality** (~10⁻¹¹ reorg/attempt); **k2 = the ONLY absolute floor**; (k2,k1] band is density-revertable. As-built k1-clamp = P-11 deadlock (the bug).
- Depths (LIVE, verify against code not audit): k1 mainnet=1024/testnet=256/dev=32; **k2=100·k1** (mainnet 102400 ≈8 days); R=round(3.1·k1); kLookback→k1+1, sWindow→~R/3. Audit's 10·k1/2550 is STALE.
- Shard/metagraph finality is **INHERITED** from the embedding gl0 ordinal (GENESIS-DENSITY §4) — NOT a parallel ladder. Reorg → deterministic replay of embedded bytes.

## 3. TRACK-1 (re-exec primary) — blockers RESOLVED
Target: `CurrencySnapshotAcceptanceManager.accept` (CSAM, accept=209-670) → pure fn of (prior, blocks, recorded globalSyncView); pin diff-base; delete authoritative override.

- **1a (RESOLVED). Scalar interval-derivation REFUTED** — permanent cross-shard-spend loss: `unappliedGlobalChangeOrdinals` (U) sourced from LOCAL HEAD (CSAM:298) grows as head advances, but `globalSyncView` runs ahead via peer-sync (no clamp, CSAM:108-118) → an ordinal enters U *below* the advanced view → scalar `(prior_view, cur]` misses it forever; cache's per-ordinal `allProcessedOrdinals` catches it. **FIX = set-valued in-band P:** `A = {o ∈ U : o ≤ view ∧ o ∉ P}`, P = ⋃ `GlobalSnapshotsProcessed.ordinals` from retained CL0 history (P is ALREADY in-band, CSAM:649-651; node-local Ref at CSAM:148 is an unnecessary mirror). **INVARIANT: P-window ⊇ U-window** (else evicted-P-still-in-U re-applies — latent bug today). Option 2 (make U-removal synchronous in-band → `A={o∈U:o≤view}`) is cleaner but a state-machine redesign → follow-up.
- **2a (RESOLVED — user: build on all 3 paths).** Pinned per-MG-Info reader (`signedBytesStore.readState`) exists only on the gl0 rail; SharedServices `createContext` (cl0/dl1 follower) path has none (GSConsensus:564-565) → delete-override would freeze followers. Build a **version-retained per-MG-Info reader on ALL 3 mptRoot paths**; serves both re-exec (committee) and byteDiff-adopt (followers).
- **3a (RESOLVED).** Deleting the override fail-closes 100% of honest sharded MGs — `reconstructInfoFromDiff` always yields `Some(empty)` (GlobalStateConverter:1477-1483) but live emits genuine `None` (`lastFeeTxRefs`=None hardcoded CSAM:623; `lastMessages`=None message-free); stateProof distinguishes them (currency.scala:106-112). **FIX = gate each GAP-1 field on `stateProof.<field>Proof.isDefined`** (not the removed `authoritative*.isDefined`). GAP-1 must bind reconstructed→tip-incremental signed stateProof (Byzantine-producer check).
- **Anchor (RESOLVED — user).** `diffBaseOrdinal` pins at OPTIMISTIC (Phase-1) finality; retention is DISK-BACKED to k2 (§5) so a liveness-degraded reorg falls back to density/length.
- **diff-base-pin.** Carry `diffBaseOrdinal` in a NEW `ShardCheckpointSigPreimageV2` (greenfield, hard cutover); both re-exec + adopt read per-MG prior at that pinned ord. NOTE: plan's `GSConsensus:1700-1703` is a per-MG hash MAP (`finalizedBasePerMgTip`), NOT a scalar — use `mptStore.lastPersistedOrdinal` captured ATOMICALLY with the diff-prior read (2d). Watchtower twin (GSConsensus:540-543) + producer (1741-1751) pin to the SAME ord atomically (2c: reshape `reExecDerivationWithDiff` closure at ShardCheckpointWiring:211-216 — currently takes no ordinal arg).
- **delete-override.** GSAM override 1066-1075 (plan said 1064-1073, +2 drift) + producer twin ShardCheckpointWiring:351-360. Both twins delete in ONE commit, ONLY after 1a+2a+diff-base-pin land (3c). Re-ground GAP-1 per 3a.
- **GATES (BUILT).** `CurrencySnapshotAcceptancePuritySuite` in worktree `agent-abe9d8c297f13ef04` (branch `worktree-agent-abe9d8c297f13ef04`): forcing-test RED-for-right-reason (only `balancesProof` diverges; head A applies cross-shard spend, head B doesn't; 8 other fields identical) + numShards=1 baseline GREEN at real mainnet fields-added-ordinals. RED until I-PIN lands. Forcing lever = spend-action VALUE via metagraphSyncData divergence (bare ordinal diff = false green).

## 4. TRACK-3 (kill k1-absolute) — blockers RESOLVED
Audit is MATERIALLY STALE (see §2 depths). RebootstrapOrchestrator default = **TRUE/ON** (docs say OFF = live factual error: application.conf:372, scaladoc, attestation-and-finality.md:630). `NAKAMOTO_ARCHIVAL_DEPTH` sys.env ALREADY deleted. Settled marker (`lastArchivalOrdinalRef`, SnapshotLeaderLoop:1067) EXISTS but fiber-local. Line drift ~+144 in SnapshotLeaderLoop. **Density GENUINELY NOT LANDED:** `ChainSelection.make` called WITHOUT kLookback/sWindow (GSConsensus:1015 → hardcoded 50/200); shouldSwitch still k1-clamps (:101-112). **This is ATTEMPT #2:** `86f390130` did this, REVERTED 2026-06-26 (`376d09fbc`) as inert+self-contradicting; real storm root = non-det smtRoot (`376d09fbc` fix) — **RE-CONFIRM 376d09fbc holds on HEAD before re-enabling deep reorgs.**

**Corrected order: S1 → S1.5 (NEW) → {S2 ∥ S3} → S4 → S5.**
- **S1 k2-unify.** Single k2 accessor = `NakamotoConfig.keepDepthBehindFinalized` (=100·k1); kill the duplicate inline `ArchivalDepthK=100*k1` (SnapshotLeaderLoop:1013); PROMOTE fiber-local `lastArchivalOrdinalRef` → injected `settledOrdinalRef`; expose `/settled` route + snapshot-info field (response-JSON ONLY — NOT in GlobalSnapshotInfo/stateProof).
- **S1.5 MARKER SPLIT (NEW, in no slice — the key fix).** `nakamotoFinalizedOrdinalRef` is ONE ref serving k1 production-guard (SnapshotLeaderLoop:1687 — MUST stay k1) + k2 store-freeze (NakamotoChainStore:384-410 — MUST move to k2) + fork-choice. Add `nakamotoSettledOrdinalRef` (k2, from T_depth2). Without it, floor-move is inert OR drags production to k2 = the 86f390130 self-contradiction.
- **S3 floor-move (4 verify-mandated ADDITIONS).** store-gate+shouldSwitch→k2 (settled ref); production floor stays k1; wire kLookback=k1+1/sWindow≈R/3 (kills 50/200); **fix densityCompare NON-COMMUTATIVITY** (ChainSelection:197-216: buildTines truncates before true MRCA at kLookback=k1+1; forkSlot anchors first-arg → compare(A,B)≠compare(B,A) → no convergence; find real MRCA, remove tineA-only asymmetry; prove cluster-uniform via §5.6 sim); CONFIG-FLAG gate defaulting to k1-freeze until the sim passes.
- **S2 retention = DISK-BACKED k2 revert (USER DIRECTION — supersedes the verify's shallow-band).** Cardano UTxO-HD analog. `MptStateStorage` is ALREADY disk-backed (per-ordinal `writeState`/`readState`, trie rebuilt on load, prunable `deleteAbove`/cutoff). **HEAP BLOWUP = in-memory `undoJournalRef: Ref[SortedMap[Long, ChangeSet]]`** (ChangeSet={upserts:Map[Hex,Bytes],removals:Set[Hex]}), k2 of those in RAM ≈5GB — NOT the base. FIX: bound `undoJournalRef` in RAM to a fraction of k (fast near-tip path); DEEP revert (to k2) loads from disk — `deleteAbove(forkOrd)`+`readState(forkOrd)`→rebuild→re-fold (machinery EXISTS: rollback/download use it); raise `signedBytesStore` `ContiguousOrdinalCutoff(signedBytesRetentionDepth)` to cover k2 (disk, cheap ~GB); density headers→k2 (tiny). RAM window=perf knob; revert depth=k2 (disk). **RE-VERIFY** disk-backed deep-revert determinism (readState+re-fold = byte-identical base) as the FIRST S4 TDD gate.
- **S4 revert-executor.** public `MptOverlay.revertToOrdinal` (descending `applyUndoAt` for shallow, or disk `readState` for deep; fail-closed on journal gap; shared mutex) + `EtaStateManager.forgetUncommitted` (`walkCacheRef.set(empty)`, :93 promises-but-unimplemented) wired into ALL revert paths (MptOverlay:746/775 reorg arms + follower resync StateChannel:236/CurrencySnapshotProcessor:159). Eta RECOMPUTES via bootstrap-equivalence. cl1 self-heals on OVERTAKE only (:177 forward-guard — do NOT force backward-resync, reopens #122).
- **S5 p11-retire.** Docs NOW (fix Default-OFF→ON factual error). Config flip `rebootstrap-enabled` true→false HARD-GATED on S3+S4 e2e-green; RETAIN the mechanism as manual escape hatch.

## 5. USER DECISIONS (settled — do NOT re-litigate)
1. Symmetric per-MG-Info reader on ALL 3 paths. 2. Anchor: optimistic finalize + disk-backed k2 retention. 3. Adoption: role-split (committee/watchtowers re-exec; everyone else byteDiff). 4. Retention: **disk-backed k2 revert (NOT shallow band)** — Cardano UTxO-HD analog. 5. k2=100·k1 is intended.

## 6. KEY FILES
CSAM `modules/node-shared/.../managers/currency/CurrencySnapshotAcceptanceManager.scala`; GSOM `.../currency/GlobalSnapshotOpsManager.scala` (P/cache); GSAM `.../managers/global/GlobalSnapshotAcceptanceManager.scala`; GSCEP `.../snapshot/GlobalSnapshotStateChannelEventsProcessor.scala`; ShardCheckpoint `modules/shared/.../schema/sharding/ShardCheckpoint.scala` + `ShardDerivedStateDelta.scala`; ShardCheckpointWiring/Producer `.../infrastructure/sharding/`; MptOverlay `.../domain/nakamoto/overlay/MptOverlay.scala` (undoJournalRef:502, applyUndoAt:955) + `ChangeSet.scala`; MptStateStorage `modules/shared/.../security/mpt/storages/MptStateStorage.scala` (disk); ChainSelection/NakamotoChainStore/SnapshotLeaderLoop/TipTracker/FinalityTrigger under dag-l0+node-shared nakamoto; NakamotoConfig `modules/node-shared/.../config/types.scala`.

## 7. NEXT STEPS (pick up here)
1. This doc = the consolidated spec. Optionally rewrite SHARDING-PRODUCTION-READINESS-PLAN §3 to match (or just reference this).
2. Implement in parallel worktree streams: **Track-1** (`I-PIN set-valued-P` ∥ `diff-base-pin` → 3-path reader → delete-override) — forcing-test worktree `agent-abe9d8c297f13ef04` is the TDD target; **Track-3** (S1→S1.5→{S2∥S3}→S4→S5).
3. First gates: (a) re-confirm `376d09fbc` smtRoot determinism holds on HEAD; (b) disk-backed deep-revert determinism (S4).
4. Drive via agents (worktree-isolated), review each diff, adversarially verify against the determinism invariants. Tracks 1 & 3 are file-disjoint → parallel-safe.
